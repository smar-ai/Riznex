import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const client = await prisma.client.findFirst(); if (!client) return console.log('No client found'); await prisma.expense.create({ data: { clientId: client.id, category: 'herbies_head_office', subcategory: 'Herbies Head Office Marketing', amount: 30, period: 'template', date: new Date() } }); console.log('Template added'); } main().catch(console.error).finally(() => prisma.$disconnect());
